spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/beer_order_service?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: beer_order
    password: password
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

logging:
  level.org.hibernate.SQL: DEBUG